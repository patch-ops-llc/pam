# ============================================================
# PAM (PatchOps Agency Manager) - Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# On Railway, set these in the service's Variables tab.
# ============================================================

# --- Required ---

# PostgreSQL connection string (Neon, Railway Postgres, or any PG provider)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# Session encryption secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your-session-secret-here

# Application URL (your production domain, used for OAuth callbacks & cookies)
APP_URL=https://work.patchops.io

# --- Email (SendGrid) ---

# SendGrid API key (get from https://app.sendgrid.com/settings/api_keys)
SENDGRID_API_KEY=SG.xxxxxxxxxxxx
# Verified sender email address in SendGrid
SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# --- Google OAuth (SSO login + Calendar integration) ---

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# --- AI (Anthropic Claude for proposal generation) ---

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# --- Google Cloud Storage (file uploads, logos) ---

# Option A: Paste the full JSON service account key (recommended for Railway)
GCS_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"...","client_email":"..."}
# Option B: Set the project ID and use GOOGLE_APPLICATION_CREDENTIALS file path
# GCS_PROJECT_ID=your-gcp-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Public object storage paths (comma-separated bucket paths for public assets)
PUBLIC_OBJECT_SEARCH_PATHS=/your-bucket-name/public
# Private object storage directory
PRIVATE_OBJECT_DIR=/your-bucket-name/private

# --- UAT White-Label Domain ---

UAT_CUSTOM_DOMAIN=https://testhub.us
VITE_UAT_CUSTOM_DOMAIN=https://testhub.us

# --- Optional ---

# Server port (Railway sets this automatically; default 5000 for local dev)
# PORT=5000
# Node environment (set automatically in build scripts)
# NODE_ENV=production
